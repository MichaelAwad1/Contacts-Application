import { ValueFieldAcceptInputEvent, ValueFieldAdapter } from '../../../index';
export declare class DateFieldAdapter extends ValueFieldAdapter {
    static PROPERTIES_ORDER: string[];
    protected _onWidgetAcceptInput(event: ValueFieldAcceptInputEvent<Date>): void;
    /**
     * Make sure hasDate and hasTime are always set before displayText, otherwise toggling hasDate and hasTime dynamically
     * won't work because renderDisplayText would try to write the time into the date field
     */
    protected _orderPropertyNamesOnSync(newProperties: Record<string, any>): string[];
    static isDateAllowedRemote(date: Date): boolean;
    static modifyPrototype(): void;
}
//# sourceMappingURL=DateFieldAdapter.d.ts.map