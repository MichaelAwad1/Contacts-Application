import { FileChooserField, PropertyChangeEvent, ValueFieldAdapter } from '../../../index';
export declare class FileChooserFieldAdapter extends ValueFieldAdapter {
    widget: FileChooserField;
    static PROPERTIES_ORDER: string[];
    protected _onWidgetPropertyChange(event: PropertyChangeEvent<any, FileChooserField>): void;
    protected _onValueChange(event: PropertyChangeEvent<File, FileChooserField>): void;
    protected _syncDisplayText(displayText: string): void;
    /**
     * Handle events in this order value, displayText. This allows to set a null value and set a display-text
     * anyway. Otherwise the field would be empty. Note: this order is not a perfect solution for every case,
     * but it solves the issue reported in ticket #290908.
     */
    protected _orderPropertyNamesOnSync(newProperties: Record<string, any>): string[];
}
//# sourceMappingURL=FileChooserFieldAdapter.d.ts.map