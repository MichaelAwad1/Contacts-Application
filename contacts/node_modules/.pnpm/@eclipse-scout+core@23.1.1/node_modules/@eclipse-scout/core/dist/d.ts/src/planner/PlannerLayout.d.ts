import { AbstractLayout, Planner } from '../index';
export declare class PlannerLayout extends AbstractLayout {
    planner: Planner;
    constructor(planner: Planner);
    layout($container: JQuery): void;
    /**
     * Min width is necessary for horizontal scrollbar
     */
    protected _updateMinWidth(): void;
    /**
     * Positions the scale lines and set to correct height
     */
    protected _layoutScaleLines(): void;
    protected _minWidth(): number;
}
//# sourceMappingURL=PlannerLayout.d.ts.map